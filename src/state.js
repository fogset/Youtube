import { atom, selector } from "recoil";

export const recoilUserIndex = atom({
    key: "recoilUserIndex",
    default: "",
});
export const recoilPage1 = atom({
    key: "recoilPage1",
    default: "",
});
